<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permisos necesarios para el acceso a Internet y el estado de la red -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Uniformes2"
        tools:targetApi="31">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleInstance">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Actividad MenuPrincipalActivity -->
        <activity
            android:name=".MenuPrincipalActivity"
            android:exported="true">
        </activity>

        <!-- Actividad Escuelas -->
        <activity
            android:name=".EscuelaCRUD"
            android:exported="true">
        </activity>
        <activity
            android:name=".EscuelaAgregar"
            android:exported="true">
        </activity>
        <activity
            android:name=".EscuelaModificar"
            android:exported="true">
        </activity>
        <activity
            android:name=".MostrarEscuela"
            android:exported="true">
        </activity>

        <!-- Actividad Tallas -->
        <activity
            android:name=".MostrarTallas"
            android:exported="true">
        </activity>
        <activity
            android:name=".TallaCRUD"
            android:exported="true">
        </activity>
        <activity
            android:name=".TallasAgregar"
            android:exported="true">
        </activity>
        <activity
            android:name=".TallasModificar"
            android:exported="true">
        </activity>

        <!-- Actividad Prendas -->
        <activity
            android:name=".MostrarPrendas"
            android:exported="true">
        </activity>
        <activity
            android:name=".PrendasCRUD"
            android:exported="true">
        </activity>
        <activity
            android:name=".PrendasAgregar"
            android:exported="true">
        </activity>
        <activity
            android:name=".PrendasModificar"
            android:exported="true">
        </activity>
    </application>

</manifest>
